# Build from the root of the repo to include shared/
FROM node:20-slim
WORKDIR /app
COPY shared/ ./shared/
COPY relay/package*.json ./relay/
WORKDIR /app/relay
RUN npm install --production
COPY relay/ .
EXPOSE 8080
CMD ["node", "index.js"]
