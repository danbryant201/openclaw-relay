{
  "name": "bridge",
  "version": "1.0.0",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js",
    "start:local": "cross-env RELAY_URL=ws://localhost:8080 node bridge.js",
    "test": "node --test sessions.test.js memory-agent.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Local bridge for openclaw-relay",
  "dependencies": {
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
